# Core
pandas>=2.0
numpy>=1.24
pyarrow>=14.0          # parquet I/O

# LLM client (Stages 1 and 2). Talks to any OpenAI-compatible server, e.g. vLLM.
openai>=1.40

# Stage 3 classifier and features
scikit-learn>=1.3
scipy>=1.10
sentence-transformers>=2.6
joblib>=1.3

# Note: serving the LLM (vllm) is intentionally NOT pinned here. Install vLLM
# separately, matched to your CUDA/driver version:
#   pip install vllm
